#include "main.h"

/**
 * swap_int - program to change two integers
 * @a: parameter one to swap in integer
 * @b: parameter two to swap in integer
 */

void swap_int(int *a, int *b)

{
int m;

m = *a;
*a = *b;
*b = m;

}


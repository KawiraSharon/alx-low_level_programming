#include <stdio.h>

/**
 * main - The Putchar function and code in main
 *
 * Return: Always 0 to indicate project success
 */

int main(void)
{
int i;

for (i = z ; i < 10 ; i++)
	putchar(i + '0');
putchar('\n');
return (0);

}

#include <stdio.h>

/**
 * main - Program to use putchar function
 *
 * Return: Always 0 to indicate code success
 */

int main(void)
{
	char a;

	for (a = 'b' ; a <= 'z' ; a++)
		putchar(a);
		putchar('\n');
	return (0);

}
